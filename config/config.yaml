# Configuration file for Census Income Prediction Project

# Data paths
data:
  train_path: "data/census_income_learn.csv"
  test_path: "data/census_income_test.csv"
  metadata_path: "data/census_income_metadata.txt"

# Preprocessing
preprocessing:
  target_column: "income"
  test_size: 0.2
  random_state: 42
  handle_missing: "mode"  # Options: "mode", "median", "drop", "impute"
  encode_categorical: "onehot"  # Options: "onehot", "label", "target"
  scale_numerical: true
  scaler_type: "standard"  # Options: "standard", "minmax", "robust"

# Feature engineering
feature_engineering:
  create_interactions: false
  polynomial_features: false
  # Feature selection (based on EDA importance)
  feature_selection:
    enabled: true
    importance_path: "../results/reports/feature_importance.csv"  # Path to importance file from EDA
    n_features: 30  # Number of top features to select

# Modeling
modeling:
  model_type: "lightgbm"  # Options: "lightgbm", "random_forest"
  cv_folds: 5
  scoring_metric: "roc_auc"  # Options: "roc_auc", "f1", "precision", "recall", "accuracy"
  n_jobs: -1
  random_state: 42
  # Hyperparameter tuning settings (for Optuna-based models like LightGBM)
  hpt:
    n_trials_random: 50
    n_trials_tpe: 20

# Output paths
output:
  models_dir: "models/"
  results_dir: "results/"
  figures_dir: "results/figures/"
  reports_dir: "results/reports/"
  predictions_dir: "results/predictions/"

